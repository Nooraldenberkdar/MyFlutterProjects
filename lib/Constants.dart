import 'package:flutter/material.dart';

const kLabelTextStyle = TextStyle(fontSize: 18, color: Colors.grey);
const kCardColor = Color(0XFF1D1E33); //or activeColor
const kInactiveCardColor = Color(0xFF111328);
const kNumberTextStyle = TextStyle(fontSize: 50.0, fontWeight: FontWeight.w900);
const kBottomContainerStyle = TextStyle(fontSize: 30, color: Colors.white);
const kTitleTextStyle = TextStyle(fontSize: 50.0, fontWeight: FontWeight.bold);
const kStatusTextStyle = TextStyle(color: Color(0xFF24D876), fontSize: 22.0, fontWeight: FontWeight.bold);
const kBmiTextStyle = TextStyle(fontSize: 100.0, fontWeight: FontWeight.bold);
const kDescription = TextStyle(fontSize: 22);

// we add k before any constant because if we want to add
//constant value to any property we will write k and see all constant we have
